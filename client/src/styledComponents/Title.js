import styled from 'styled-components';

export default styled.div`
  grid-column: 1 / 3;
  grid-row: 1 / 2;
  justify-self: start;
  padding: 0 0 10px;
  font-family: 'Lato', sans-serif;
  font-weight: 700;
  font-size: 16px;
  color: #2c2c2c;
`;
